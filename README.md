## install

php 8.2 needs to be installed alongside with composer

```bash
composer install
npm install
cp .env.example .env
php artisan key:generate
```

## run

```bash
terminal 1:
php artisan serve

terminal 2:
npm run dev
```
